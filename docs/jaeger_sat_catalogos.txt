Estructura del proyecto
.
└── Jaeger
    └── SAT
        ├── CFDI
        │   ├── API
        │   │   └── Consulta de Estado
        │   └── Reader
        └── Catálogos
            ├── Helpers
            ├── Repository
            │   ├── Abstracts
            │   └── Interfaces
            ├── Update
            │   ├── Converts
            │   └── Importers
            └── Scraping
                ├── Abstracts
                ├── Entities
                ├── Helpers
                └── Interfaces




IRepositoryGeneric {
    Version:string
    Title:string
    Lastupdate:date
    Builder:string
    Load();
    Save();
    Import(DataTable);
}

IRepositoryContext<T> : IReposityGeneric {
    FileName:string
    Revision:string
    WorkingFolder:string
    Items:List<T>
    T Search(string query);
    Restore();
    ZIP();
}

RepositoryContext : IRepositoryContext<T> : IReposityGeneric  where T : class, new() {

}